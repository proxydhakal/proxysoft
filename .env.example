# Django
SECRET_KEY=your-secret-key-min-50-chars-use-get-random-secret-key
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
ADMIN_URL=admin/

# Email (contact form auto-reply & outbound mail)
DEFAULT_FROM_EMAIL=noreply@proxysoft.com
SERVER_EMAIL=noreply@proxysoft.com
# Comma-separated staff inboxes for "new inquiry" alerts (default: proxydhakal@gmail.com)
CONTACT_INQUIRY_NOTIFICATION_EMAILS=proxydhakal@gmail.com,other@example.com
# Local dev: leave unset to print emails to the console (default).
# To test SMTP locally, set e.g.:
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_USE_TLS=true
EMAIL_HOST_USER=your_smtp_user
EMAIL_HOST_PASSWORD=your_smtp_password

# Database - Local (SQLite used when DJANGO_ENV is not production)
# No DB_* vars needed for SQLite

# Database - Production (MySQL) - set DJANGO_ENV=production
SECRET_KEY=2p7&z3^k_al_2uuk3lsakx2!4@rt64ddjth=dsjbj&p_jhkab8
DJANGO_ENV=production
DEBUG=False
ADMIN_URL=admin/
ALLOWED_HOSTS=proxysoft.com.np,www.proxysoft.com.np
DB_NAME=shekhard_proxysoft
DB_USER=shekhard_proxy
DB_PASSWORD=SpringWinter!
DB_HOST=localhost
DB_PORT=3306
# HTTPS (required for admin/forms behind TLS; comma-separated)
CSRF_TRUSTED_ORIGINS=https://proxysoft.com.np,https://www.proxysoft.com.np

# Optional production
STATIC_ROOT=/home/shekhard/proxysoft.com.np/staticfiles
MEDIA_ROOT=/home/shekhard/proxysoft.com.np/media
# Static and media: allow env override; default to cPanel public_html paths when not set
_default_static_root = os.getenv('STATIC_ROOT', '/home/shekhard/public_html/staticfiles')
_default_media_root = os.getenv('MEDIA_ROOT', '/home/shekhard/public_html/media')
STATIC_ROOT = Path(_default_static_root)
MEDIA_ROOT = Path(_default_media_root)
STATIC_URL = os.getenv('STATIC_URL', '/static/')
MEDIA_URL = os.getenv('MEDIA_URL', '/media/')
SECURE_SSL_REDIRECT=True

# Verify production security (needs all MySQL vars above):
#   DJANGO_ENV=production python manage.py check --deploy

# Production email (SMTP) — also set DEFAULT_FROM_EMAIL to a valid sender for your provider
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=mail.proxysoft.com.np
EMAIL_PORT=587
EMAIL_USE_TLS=true
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=noreply@proxysoft.com.np
SERVER_EMAIL=noreply@proxysoft.com.np
